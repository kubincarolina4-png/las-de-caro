# Copiá este archivo a .env.local y completá con los datos reales
# de tu proyecto de Supabase (Settings → API).
# .env.local NO se sube a GitHub (está en .gitignore).

VITE_SUPABASE_URL=https://tu-proyecto.supabase.co
VITE_SUPABASE_ANON_KEY=tu-clave-publica-anon-aqui
